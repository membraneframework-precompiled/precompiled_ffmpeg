version: 2.1

jobs:
  build:
    docker:
      - image: ubuntu
    steps:
      - checkout
      - run: apt update
      - run: yes | apt install build-essential curl
      - run: ./build-ffmpeg --build

workflows:
  build:
    jobs:
      - build